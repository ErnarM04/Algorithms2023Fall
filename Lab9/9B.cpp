#include <bits/stdc++.h>
using namespace std;
int cnt(string a, string b){
	for(int i = 0; i < b.size(); i++){
		if(b[i])
	}
}
int main() {
	string a, b;
	int n;
	cin >> a >> n >> b;
	if(count(b.begin(), b.end(), a) >= n) cout << "YES";
	else cout << "NO";
}

